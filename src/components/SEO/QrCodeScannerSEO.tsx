
import React from 'react';
import SEOMetadata from './SEOMetadata';

const QrCodeScannerSEO = () => {
  return (
    <SEOMetadata 
      title="QR Code Scanner | Scan QR Codes from Images or Webcam"
      description="Use our free QR code scanner to scan and decode QR codes from your webcam or uploaded images. Works on mobile and desktop devices."
      keywords="qr code scanner, scan qr codes, qr code reader, webcam qr scanner, qr code decoder, mobile qr code scanner"
      canonicalUrl="/qr-code-scanner"
    />
  );
};

export default QrCodeScannerSEO;
